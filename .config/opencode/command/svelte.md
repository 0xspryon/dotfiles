---
description: Get up-to-date information about Svelte
agent: codebase-docs-agent
---

You have access to the Svelte and SvelteKit documentation/source at:

`/run/media/hbt/work/01-better-agents-codebases/resources/svelte.dev`

Use this codebase to provide accurate, up-to-date information about Svelte, SvelteKit, and related tools.

Search docs first unless implementation details are needed. Read small sections at a time and avoid loading many files at once.

For Svelte code in chat:

- If outputting only script content, use a `typescript` code block and do not include the `<script>` tag.
- If outputting a full Svelte component, use an `html` code block.
- Prefer script-only answers unless markup or styles are necessary.
